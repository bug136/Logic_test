# STM32F103C8 逻辑分析仪测量说明

芯片：STM32F103C8，主频 **72 MHz**  
工程：`Logic_pwm_uart_iic_spi`（标准外设库）  
软件：Saleae Logic 2，采样率建议 **16 MS/s**  
电平 **3.3 V**，分析仪 **GND 必须接开发板 GND**。

本工程同时跑五类波形，方便对照代码和总线：

| 外设 | 引脚 | 作用 |
|------|------|------|
| 定时器 | PA0 | TIM2_CH1，50 Hz、占空比 50% |
| PWM | PA1 | TIM2_CH2，50 Hz、占空比 25% |
| UART | PA9 / PA10 | USART1 发/收，115200 8N1 |
| I2C | PB6 / PB7 | I2C1 ↔ 24C08，读 `"hello"` |
| SPI | PA4–PA7 | SPI1 ↔ W25Q64，读 JEDEC ID `EF 4017` |

PWM **连续输出**；UART / I2C / SPI 在 `while(1)` 里一轮一轮发，中间有约 200 ms 间隔。看通信波形时，先把时间轴缩到 **100 ms～1 s**，不要只抓几毫秒。

---

## 1. 接线总表

### 1.1 接到逻辑分析仪

| 分析仪 | STM32 | 信号 | 空闲电平 | 预期 |
|--------|-------|------|----------|------|
| GND | GND | 地 | — | — |
| D0 | **PA0** | TIM2_CH1 | 高低交替 | 50 Hz，占空比 50% |
| D1 | **PA1** | TIM2_CH2 | 高低交替 | 50 Hz，占空比 25% |
| D2 | **PA9** | USART1_TX | **高** | 115200 |
| （另接） | **PA10** | USART1_RX | **高** | 须短接 PA2→PA10 |
| D3 | **PB6** | I2C1_SCL | **高**（须上拉） | 100 kHz |
| D4 | **PB7** | I2C1_SDA | **高**（须上拉） | 数据 |
| D5 | **PA4** | SPI1 CS | **高** | 低有效 |
| D6 | **PA5** | SPI1 SCK | **低**（Mode 0） | ≈281 kHz |
| D7 | **PA7** | SPI1 MOSI | 随数据 | 主机输出 |

SPI 还要看从机回读时，把一根探针接到 **PA6（MISO）**。通道不够就分两次抓。

### 1.2 外设之间的短接 / 芯片接线

**串口自发自收（测 RX 必须接）：**

```text
PA2 (USART2_TX)  ——杜邦线——  PA10 (USART1_RX)
```

也可用 USB 转串口：模块 TX→PA10、RX→PA9、GND 共地，115200 8N1。

**I2C ↔ 24C08：**

| 24C08 | STM32 |
|-------|-------|
| SCL | PB6 |
| SDA | PB7 |
| VSS | GND |
| VCC | 3.3 V（若是 5 V 旧片见芯片手册） |
| WP | GND（接高则不能写） |
| A0/A1/A2 | 可不接或接 GND |

SCL、SDA 各接 **4.7 kΩ 上拉到 3.3 V**（模块板载有则可不再加）。空闲时 PB6、PB7 必须为高。

**SPI ↔ W25Q64BV（3.3 V）：**

| W25Q64 | STM32 |
|--------|-------|
| /CS | PA4 |
| CLK | PA5 |
| DO | PA6（MISO） |
| DI | PA7（MOSI） |
| VCC / GND | 3.3 V / GND |
| /WP、/HOLD | 3.3 V |

DO 接 PA6、DI 接 PA7，反了只能读到 `FF`。

---

## 2. 定时器（PA0 / TIM2_CH1）

代码：`Timer_PWM_Init()`。

```text
72 MHz / (71+1) = 1 MHz     → 1 个计数 = 1 µs
ARR = 19999                 → 周期 20000 µs = 20 ms → 50 Hz
CCR1 = 10000                → 高电平 10 ms，低电平 10 ms → 占空比 50%
```

PWM1 模式：计数小于 CCR 时输出高。

### 期望波形

```text
PA0  ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾          ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
     |    10 ms     |  10 ms  |
     周期 20 ms  →  50 Hz，占空比 50%
```

### 怎么判断对

- 频率约 **50 Hz**，周期约 **20.0 ms**
- 高电平约 **10.0 ms**，占空比 **50%**
- 与 PA1 **上升沿对齐**（同一颗 TIM2）

---

## 3. PWM（PA1 / TIM2_CH2）

同一颗 TIM2，只是比较值不同：`CCR2 = 5000`。

```text
高电平 5 ms / 周期 20 ms = 25%
频率仍是 50 Hz
```

### 期望波形

```text
PA0  ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾          ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾     50%
PA1  ‾‾‾‾‾‾‾‾                  ‾‾‾‾‾‾‾‾               25%
     |  5 ms  |
```

### 怎么判断对

- 频率与 PA0 相同
- 高电平约 **5.0 ms**，占空比 **25%**
- 边沿与 PA0 对齐

---

## 4. UART（PA9 TX / PA10 RX）

参数：**115200，8 数据位，无校验，1 停止位，LSB first**。空闲为高，起始位拉低。

代码每轮循环（约 200 ms）会发三截：

| 顺序 | 引脚 | 内容 | 来源 |
|------|------|------|------|
| 1 | PA9 | `EEPROM hello\r\n` | 刚读完 24C08 |
| 2 | PA9 | `U STM32 UART\r\n` | 固定字符串 |
| 3 | PA2→PA10 | `AA 55 52 58 0D 0A` | USART2 制造 RX |
| 4 | PA9 | `AA 55 52 58 0D 0A` | 把 RX 回显出去 |
| 5 | PA9 | `JEDEC EF 4017 OK\r\n` | 读 W25Q64 ID |

`52 58` 是字符 `'R'` `'X'`。

### 期望波形（TX / PA9）

空闲高。缩小时间轴后，每隔约 200 ms 一簇下降沿脉冲。

Logic 2：Analyzers → **Async Serial**，Bit Rate = **115200**，8N1，LSB first。

第一簇应解出 `EEPROM hello`，后面紧跟 `U STM32 UART`，再跟回显 `AA 55 RX\r\n`，再跟 `JEDEC EF 4017 OK`。

### 期望波形（RX / PA10）

探针改接 **PA10**，必须短接 **PA2→PA10**。同样 115200，应解出：

```text
AA 55 52 58 0D 0A
```

### 触发

不要用 PA0（PWM 一直在跳）。看发送：PA9 **下降沿**；看接收：PA10 **下降沿**。

---

## 5. I2C（PB6 SCL / PB7 SDA ↔ 24C08）

时钟 **100 kHz**。器件 8 位地址：写 **`0xA0`**，读 **`0xA1`**（Logic 2 常显示 7 位地址 **`0x50`**）。

片内字地址 8 位。上电写一次 `"hello"` 到地址 0，循环里只做**随机读 5 字节**。

### 5.1 上电写（只出现一次）

页写，最多 16 字节，本工程写 5 字节：

```text
START → Write 0xA0 ACK → 字地址 0x00 ACK
      → 68('h') ACK → 65('e') ACK → 6C('l') ACK → 6C('l') ACK → 6F('o') ACK
      → STOP
```

写完芯片内部编程，约 **2～10 ms** 不应答，然后才允许再访问。

### 5.2 循环读（主要抓这个）

随机读 + 顺序读：

```text
START → Write 0xA0 ACK → 字地址 0x00 ACK
Restart → Read 0xA1 ACK
      → 68('h') ACK → 65('e') ACK → 6C('l') ACK → 6C('l') ACK → 6F('o') NACK
      → STOP
```

前 4 字节主机 ACK，芯片继续吐；最后一字节主机 **NACK** 再 STOP。  
读完后 **不应再多一字节 `0xFF`**。若出现 `FF`，是主机在 STOP 前多打了时钟（已在 `M24C08_Mem_Read` 里按“先 STOP 再读最后一字节”修过）。

### Logic 2

Analyzers → **I2C**，SCL = PB6 那路，SDA = PB7 那路。  
空闲必须双线为高。触发用 SDA **下降沿**（START）。

### 怎么判断对

- 地址 `0x50` Write，然后 `0x00`
- Restart 后 `0x50` Read
- 数据 `68 65 6C 6C 6F`，最后 `6F` 为 **NAK**
- 串口打印 `EEPROM hello`

---

## 6. SPI（PA4 CS / PA5 SCK / PA7 MOSI / PA6 MISO ↔ W25Q64）

| 项 | 值 |
|----|----|
| 模式 | **Mode 0**（CPOL=0，CPHA=0） |
| 空闲 SCK | **低** |
| 采样沿 | **上升沿** |
| 位序 | MSB first |
| 速率 | 72 MHz / 256 ≈ **281 kHz** |
| CS | 软件控制，空闲高，一帧内保持低 |

循环里每次调用 `W25Q64_ReadID()`：发 JEDEC 命令 `9Fh`，回 3 字节 ID。

### 期望波形（读 ID）

```text
CS    ‾‾‾\________________________________/‾‾‾
SCK       32 个时钟（4×8），空闲为低
MOSI      9F        FF        FF        FF
MISO      FF        EF        40        17
          命令      厂商      类型      容量
```

| MOSI | MISO | 含义 |
|------|------|------|
| `9F` | `FF` | 发读 ID 命令，此时芯片还没吐数 |
| `FF` | `EF` | Winbond 厂商 ID |
| `FF` | `40` | Memory Type |
| `FF` | `17` | Capacity，64 Mbit = W25Q64 |

合起来：**`EF 40 17`**，代码里拼成 `id1=0xEF`、`id2=0x4017`。

上电 `W25Q64_DemoOnce()` 还会：擦 4 KB → 写 `11 22 33 44` → 读回。串口应有 `FLASH 11 22 33 44 OK`。之后循环只重复读 ID。

### Logic 2

Analyzers → **SPI**：

- Clock = PA5，Enable = PA4（极性 **Low**）
- MOSI = PA7，MISO = PA6
- Mode = **0**，8 bit，MSB first

触发：CS **下降沿**。

### 怎么判断对

- CS 为高时 SCK 保持低（Mode 0）
- 一帧正好 32 个时钟
- 解码 `MOSI: 9F FF FF FF`，`MISO: FF EF 40 17`
- 串口 `JEDEC EF 4017 OK`

W25Q64 **只支持 Mode 0 / Mode 3**。改模式在 `Hardware/W25Q64.c` 的 `SPI_CPOL` / `SPI_CPHA`，分析仪 Mode 要改成同一个。

---

## 7. 串口预期总结果

115200 8N1，上电后大致顺序：

```text
JEDEC EF 4017 OK
FLASH 11 22 33 44 OK
EEPROM hello
U STM32 UART
AA55RX          （回显，可能显示为二进制）
JEDEC EF 4017 OK
EEPROM hello
…（每约 200 ms 重复后几行）
```

| 打印 | 含义 |
|------|------|
| `JEDEC EF 4017 OK` | W25Q64 识别成功 |
| `FLASH 11 22 33 44 OK` | 擦写读校验成功（仅上电一次） |
| `W25Q64 not found, skip R/W` | SPI 没接好或芯片无应答 |
| `EEPROM hello` | 24C08 读回成功 |
| `EEPROM` 后面是乱码或空 | I2C 没 ACK，检查上拉、WP、地址 |

---

## 8. 对照清单

| 外设 | 引脚 | 单片机发送 | 单片机接收 | 期望 |
|------|------|------------|------------|------|
| 定时器 | PA0 | 50 Hz、50% 方波（连续） | 无 | 周期 20 ms，高 10 ms |
| PWM | PA1 | 50 Hz、25% PWM（连续） | 无 | 高 5 ms |
| UART TX | PA9 | `EEPROM hello`、`U STM32 UART`、回显、`JEDEC …` | — | 115200，空闲高 |
| UART RX | PA10 | — | `AA 55 52 58 0D 0A` | 须短接 PA2 |
| I2C | PB6/PB7 | 上电写 `hello`；循环读 | 读回 `68 65 6C 6C 6F` | 最后一字节 NACK 后立刻 STOP |
| SPI | PA4/5/6/7 | `9F` + 空字节 | `EF 40 17` | Mode 0，CS 低一整帧 |

---

## 9. 建议抓包顺序

1. 先看 PA0 / PA1：50 Hz、50% 与 25%，上升沿对齐。
2. 缩小时间轴到几百毫秒，应看到 PA9、PB6/PB7、PA4 每隔约 200 ms 跳一簇。
3. UART：触发 PA9 下降沿 → Async Serial 115200 → `EEPROM hello`、`U STM32 UART`。
4. UART RX：短接 PA2→PA10，探针改 PA10 → `AA 55 52 58 0D 0A`。
5. I2C：确认空闲为高 → I2C 分析器 → 读帧 `hello`，`o` 为 NAK，后面不要有 `FF`。
6. SPI：触发 PA4 下降沿 → SPI Mode 0 → `9F` / `EF 40 17`。

一次 Capture 建议 **100 ms～1 s**。
